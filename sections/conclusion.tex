\section{Conclusion}

This assignment demonstrates how linear models (logistic regression and SVM) achieve moderate performance (87\% accuracy) on non-linearly separable data, but suffer from underfitting due to their linear decision boundaries. Feature augmentation through polynomial terms dramatically improves performance to 96.4\% accuracy by enabling the model to capture the underlying non-linear decision boundary in a higher-dimensional space.

